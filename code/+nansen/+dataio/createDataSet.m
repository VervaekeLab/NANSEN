function createDataSet(filepath, varargin)
    
    % A function for creating a dataset from different types of inputs. The
    % function should select the approprioate dataset type to use. 
    %
    %   I.e it could be a "singlefolder" dataset, a "session" dataset, a
    %   "project" dataset etc.
    
end